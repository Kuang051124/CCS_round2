/**
 * @file  task3.h
 * @brief Task 3: 双车协同 — A车慢速走Task2路径, B车跟随+距离P控制
 */

#ifndef _TASK3_H_
#define _TASK3_H_

#include "ti_msp_dl_config.h"

/* ===================================================================
 * 距离 P 控制参数
 * =================================================================== */

#define T3_DIST_TARGET      30.0f   /* 目标距离 (cm)          */
#define T3_DIST_KP          0.05f  /* P 增益 (倍率/cm误差)   */
#define T3_SPEED_INIT       1.60f   /* 初始追赶倍率 (>1)      */
#define T3_SPEED_MIN        0.50f   /* 最小速度倍率           */
#define T3_SPEED_MAX        1.60f   /* 最大速度倍率           */

/* ===================================================================
 * 停车原因 (供 pages.c 显示)
 * =================================================================== */

#define STOP_REASON_NONE    0   /* 未停车                        */
#define STOP_REASON_BT      1   /* 蓝牙 STOP from A             */
#define STOP_REASON_FORCE   2   /* 强制停车 (yaw+距离触发)        */

extern uint8_t g_stop_reason;   /* 停车原因, tasks/pages 共享   */

/* ===================================================================
 * 强制停车参数 (蓝牙失效兜底)
 * =================================================================== */

#define T3_FORCE_YAW_MIN    -180.0f  /* 强制停车: 航向差下限 (°)   */
#define T3_FORCE_YAW_MAX    -100.0f  /* 强制停车: 航向差上限 (°)   */
#define T3_FORCE_DIST_CM    35.0f   /* 强制停车: 距离阈值 (cm)    */

/* ===================================================================
 * 弧线末段减速参数 (防脱轨)
 * =================================================================== */

#define T3_SLOW_YAW_START   -160.0f /* 减速起点: 航向差, 全速          */
#define T3_SLOW_YAW_END     -180.0f /* 减速终点: 航向差, 减至 1.0 倍    */
/* yaw从-150°→-180°, tk_speed_mult 线性递减到 1.0 */

/* ===================================================================
 * API
 * =================================================================== */

/* Car A (WIT) */
void Task3A_Init(void);
uint8_t Task3A_Tick(uint8_t key);

/* Car B (MPU + 摄像头距离P控制) */
void Task3B_Init(void);
uint8_t Task3B_Tick(uint8_t key);

#endif
